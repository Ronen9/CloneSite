{
  "name": "homepage-cloner-vercel",
  "version": "1.0.0",
  "description": "Homepage cloner optimized for Vercel deployment",
  "scripts": {
    "build": "cd homepage-clone && npm install && npm run build",
    "dev": "cd homepage-clone && npm run dev",
    "vercel-build": "cd homepage-clone && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "keywords": ["firecrawl", "homepage", "clone", "react", "tailwind", "vercel"],
  "author": "",
  "license": "MIT"
}